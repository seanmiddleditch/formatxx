skip_tags: true
shallow_clone: true
image:
  - Visual Studio 2017
platform:
  - x86
  - x64
configuration:
  - Release
matrix:
  fast_finish: true
build_script:
  - cmake -Bbuild -H. && cmake --build build